# 编译产物 / 运行期依赖，从 VS 生成后手工拷入，不进仓库
*.dll
*.lib
*.exe

# 扫描出的图片
scans/

# 导出的扫描仪能力结构（capdump.go）
capdump/

# 设置项缓存（options.go）
cache/

# 临时文件（截图、抓包之类，随手放的，不进仓库）
temp/

# 日志
twain.log

# 运行时生成的配置（含本机路径，不进仓库）
scansvc.conf
# 设置项现场应急覆盖文件（唯一来源是 scanopt/default_options.jsonc，这份不进仓库）
scanner-options.jsonc
